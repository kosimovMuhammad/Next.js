export default function AnalyticsDefault() {
  return (
    <p className="text-sm text-zinc-500">
      @analytics default.js fallback — shown on a hard refresh because
      /settings doesn&apos;t exist in this slot.
    </p>
  );
}
