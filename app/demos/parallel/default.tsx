export default function ParallelChildrenDefault() {
  return (
    <p className="text-sm text-zinc-500">
      children default.js fallback — the root page has no /settings route,
      so this renders on a hard navigation to /demos/parallel/settings.
    </p>
  );
}
