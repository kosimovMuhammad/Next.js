export default function OldPage() {
  return (
    <p className="text-sm text-zinc-600 dark:text-zinc-400">
      You should never see this — proxy.ts redirects this path server-side.
    </p>
  );
}
